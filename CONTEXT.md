# 直播工具

一套运行在多台 Windows 工作机上的直播辅助工具，配套一台局域网内的报警服务器集中播报需要人工处理的情况。

## Language

### 弹窗报警

**工作机**：
运行本应用、承载一个抖音直播中控台会话的一台 Windows 机器。
_Avoid_: 客户端、节点、终端

**报警服务器**：
局域网内独立的一台机器，接收所有工作机的报警状态并统一用语音播报。
_Avoid_: 主机、中心、服务端

**验证弹窗**：
抖音在中控台上弹出的人机验证窗口，出现后直播操作会被阻塞，必须由人工消除。工作机认定它的依据是可见的 Windows 顶层窗口标题，而非页面内容——已知这会带来误检与漏检，见 ADR-0005。**与"自动弹窗"无关**，后者指自动讲解商品。
_Avoid_: 人机交互窗口、验证码框、captcha；单说"弹窗"（会与自动弹窗混淆）

**报警**：
某台工作机上验证弹窗正在出现这一事实。一台工作机在同一时刻至多有一个报警——现实中一台机器只承载一个中控台会话，不会同时弹出两个验证弹窗。
_Avoid_: 事件、告警、通知

**机器标识**：
人给工作机起的短名字，1～20 个字符，用来在语音里指认是哪台机器。纯数字时读作"N 号机器"。
_Avoid_: 机器名、别名、编号

**工作机 ID**：
工作机首次启动时生成并长期保存的唯一标识，报警服务器用它区分不同工作机。与机器标识不同：机器标识给人听，工作机 ID 给机器认，人可以随时改前者而不影响后者。
_Avoid_: 客户端 ID、设备 ID、UUID

**弹窗事件号**：
一次验证弹窗从出现到消失期间不变的标识，弹窗再次出现时重新生成。它不构成报警的身份（报警的身份是工作机 ID），只用来判断一条迟到的解除消息说的是不是当前这次弹窗。
_Avoid_: 报警 ID、会话 ID、序列号

**解除**：
工作机观察到验证弹窗已消失，从而使该工作机的报警不再成立。
_Avoid_: 取消、关闭、清除

**心跳**：
报警持续期间工作机按固定间隔重发的报警状态。连续一段时间收不到心跳，报警服务器判定该工作机失联并自动解除其报警。
_Avoid_: 保活、轮询、ping

**连接测试**：
用户在工作机设置里主动发起的一次探测，用来确认报警服务器地址和端口可达，并取回该机器标识对应的播报文案。
_Avoid_: 健康检查、探活

**重复间隔**：
同一个报警两次播报之间至少要隔多久。多台同时报警时按此间隔公平轮转，谁等得最久谁先播。
_Avoid_: 播报频率、冷却时间

**播报文案**：
报警服务器实际念出的那句话。措辞规则只由报警服务器定义，工作机不自行拼装，设置页展示的是向服务器索取的真实文案。
_Avoid_: 语音内容、提示语、TTS 文本

### 直播任务

**OBS 实时去重**：
对当前 OBS 节目场景中的指定源持续施加轻微位移、色彩与径向模糊扰动的直播任务；停止时恢复启动前的源变换并移除任务创建的滤镜。
_Avoid_: 摄像头特效、防封、画面去重

**中控台**：
直播平台提供的直播管理后台。应用通过受控 Chromium 登录并操作它，这是所有直播任务的作业面。
_Avoid_: 控制台、后台、直播间管理页

**连接**：
应用打开受控 Chromium、完成登录、进入中控台并确认可操作的整个过程。由用户点击触发，没有自动重连——连接断开后必须再次由人点击。
_Avoid_: 登录、启动、上线

**自动发言**：
按设定间隔向直播间循环发送预设话术的任务。
_Avoid_: 自动评论、自动喊话（代码中的 `autoMessage` 与 `auto-comment` 指同一件事）

**自动弹窗**：
按设定间隔和次数把商品逐个推上讲解位的任务。"弹窗"指商品讲解卡，**与验证弹窗无关**。
_Avoid_: 单说"弹窗"；商品弹窗、自动讲解（口语可用"自动讲解商品"消歧）

**评论监听**：
从平台获取直播间实时消息流的任务。它是自动回复的前提——没有监听，自动回复永远收不到任何消息。
_Avoid_: 监听评论、拉取评论、消息订阅

**自动回复**：
对监听到的消息作出反应，按关键词或 AI 生成内容回复。启用状态与评论监听相互独立，两者都开才真正会回复。
_Avoid_: 智能回复、AI 回复（AI 回复只是它的一种方式）

**自动启动**：
连接成功后自动开启自动发言、自动弹窗、评论监听和自动回复的行为。按账号配置，默认关闭。
_Avoid_: 一键启动、自动开播、批量启动

## 工作机商品更换

**商品更换请求**:
工作机向管理员发出的安排新商品请求；同一工作机同时只能有一条未完成请求，完成后才能再次发起。
_Avoid_: 商品审批、换品任务、离线操作队列

**商品关联**:
管理员为一条未完成的商品更换请求选择一个商品；完成前重新关联会覆盖原商品。
_Avoid_: 商品复制、素材复制

**商品更换记录**:
从请求、商品关联到现场人员确认更换成功的完整记录；完成记录永久保留在商品元素列表中。
_Avoid_: 下载记录、运行日志、工作机连接记录

**更换状态**:
商品更换记录的待关联商品、待工作机更换或更换成功阶段；无记录时为正常。
_Avoid_: WebSocket 连接状态、下载状态

**商品元素**:
客户端中申请更换商品、查看关联商品、下载素材并确认更换成功的功能页面名称，不是独立的数据实体。
_Avoid_: 商品实体、素材实体
